import React from 'react';
import { Row, Col } from '../Grid';

export default function Card({ title }) {
    return (
        <div className="card">
            <div></div>
            
        </div>
    )
}

function CardHeader()